<!-- compound-plugin:cursor-command generated -->

# agent-native-audit

Invokes **`agent-native-audit`** from `compound-engineering`. Slash command: `/agent-native-audit`.

1. From the repository root, read **`plugins/compound-engineering/skills/agent-native-audit/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `agent-native-audit`
