<!-- compound-plugin:cursor-command generated -->

# ce:compound-refresh

Invokes **`ce:compound-refresh`** from `compound-engineering`. Slash command: `/ce-compound-refresh`.

1. From the repository root, read **`plugins/compound-engineering/skills/ce-compound-refresh/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `ce-compound-refresh`
