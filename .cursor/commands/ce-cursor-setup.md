# Cursor plugin setup

Install Compound Engineering slash commands for **any** Cursor workspace (minimal token footprint — run the script, do not hand-roll steps).

From the repository root:

```bash
npm run install:cursor
```

When the script finishes, quit Cursor fully (**Cmd+Q**) and reopen.
