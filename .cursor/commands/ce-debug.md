<!-- compound-plugin:cursor-command generated -->

# ce-debug

Invokes **`ce-debug`** from `compound-engineering`. Slash command: `/ce-debug`.

1. From the repository root, read **`plugins/compound-engineering/skills/ce-debug/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `ce-debug`
