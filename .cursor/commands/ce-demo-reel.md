<!-- compound-plugin:cursor-command generated -->

# ce-demo-reel

Invokes **`ce-demo-reel`** from `compound-engineering`. Slash command: `/ce-demo-reel`.

1. From the repository root, read **`plugins/compound-engineering/skills/ce-demo-reel/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `ce-demo-reel`
