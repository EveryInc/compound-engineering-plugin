<!-- compound-plugin:cursor-command generated -->

# ce:diagram

Invokes **`ce:diagram`** from `excalidraw-diagrams`. Slash command: `/ce-diagram`.

1. From the repository root, read **`plugins/excalidraw-diagrams/skills/ce-diagram/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `ce-diagram`
