<!-- compound-plugin:cursor-command generated -->

# ce-optimize

Invokes **`ce-optimize`** from `compound-engineering`. Slash command: `/ce-optimize`.

1. From the repository root, read **`plugins/compound-engineering/skills/ce-optimize/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `ce-optimize`
