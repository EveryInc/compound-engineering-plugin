<!-- compound-plugin:cursor-command generated -->

# ce:plan

Invokes **`ce:plan`** from `compound-engineering`. Slash command: `/ce-plan`.

1. From the repository root, read **`plugins/compound-engineering/skills/ce-plan/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `ce-plan`
