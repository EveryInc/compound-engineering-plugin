<!-- compound-plugin:cursor-command generated -->

# ce-update

Invokes **`ce-update`** from `compound-engineering`. Slash command: `/ce-update`.

1. From the repository root, read **`plugins/compound-engineering/skills/ce-update/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `ce-update`
