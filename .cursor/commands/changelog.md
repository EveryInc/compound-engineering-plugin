<!-- compound-plugin:cursor-command generated -->

# changelog

Invokes **`changelog`** from `compound-engineering`. Slash command: `/changelog`.

1. From the repository root, read **`plugins/compound-engineering/skills/changelog/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `changelog`
