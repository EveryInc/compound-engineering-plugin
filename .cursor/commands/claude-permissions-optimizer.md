<!-- compound-plugin:cursor-command generated -->

# claude-permissions-optimizer

Invokes **`claude-permissions-optimizer`** from `compound-engineering`. Slash command: `/claude-permissions-optimizer`.

1. From the repository root, read **`plugins/compound-engineering/skills/claude-permissions-optimizer/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `claude-permissions-optimizer`
