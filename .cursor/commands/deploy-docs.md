<!-- compound-plugin:cursor-command generated -->

# deploy-docs

Invokes **`deploy-docs`** from `compound-engineering`. Slash command: `/deploy-docs`.

1. From the repository root, read **`plugins/compound-engineering/skills/deploy-docs/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `deploy-docs`
