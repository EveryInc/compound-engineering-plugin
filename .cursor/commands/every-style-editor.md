<!-- compound-plugin:cursor-command generated -->

# every-style-editor

Invokes **`every-style-editor`** from `compound-engineering`. Slash command: `/every-style-editor`.

1. From the repository root, read **`plugins/compound-engineering/skills/every-style-editor/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `every-style-editor`
