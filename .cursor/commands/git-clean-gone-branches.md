<!-- compound-plugin:cursor-command generated -->

# git-clean-gone-branches

Invokes **`git-clean-gone-branches`** from `compound-engineering`. Slash command: `/git-clean-gone-branches`.

1. From the repository root, read **`plugins/compound-engineering/skills/git-clean-gone-branches/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `git-clean-gone-branches`
