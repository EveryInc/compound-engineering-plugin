<!-- compound-plugin:cursor-command generated -->

# onboarding

Invokes **`onboarding`** from `compound-engineering`. Slash command: `/onboarding`.

1. From the repository root, read **`plugins/compound-engineering/skills/onboarding/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `onboarding`
