<!-- compound-plugin:cursor-command generated -->

# test-browser

Invokes **`test-browser`** from `compound-engineering`. Slash command: `/test-browser`.

1. From the repository root, read **`plugins/compound-engineering/skills/test-browser/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `test-browser`
