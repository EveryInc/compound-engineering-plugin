<!-- compound-plugin:cursor-command generated -->

# test-xcode

Invokes **`test-xcode`** from `compound-engineering`. Slash command: `/test-xcode`.

1. From the repository root, read **`plugins/compound-engineering/skills/test-xcode/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `test-xcode`
