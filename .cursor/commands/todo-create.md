<!-- compound-plugin:cursor-command generated -->

# todo-create

Invokes **`todo-create`** from `compound-engineering`. Slash command: `/todo-create`.

1. From the repository root, read **`plugins/compound-engineering/skills/todo-create/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `todo-create`
