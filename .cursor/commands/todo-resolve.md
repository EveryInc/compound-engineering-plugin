<!-- compound-plugin:cursor-command generated -->

# todo-resolve

Invokes **`todo-resolve`** from `compound-engineering`. Slash command: `/todo-resolve`.

1. From the repository root, read **`plugins/compound-engineering/skills/todo-resolve/SKILL.md`** and execute that skill end-to-end.
2. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `todo-resolve`
