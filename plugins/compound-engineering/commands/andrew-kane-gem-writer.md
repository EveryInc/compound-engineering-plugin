<!-- compound-plugin:cursor-command generated -->

# andrew-kane-gem-writer

Invokes **`andrew-kane-gem-writer`** (compound-engineering). Slash command: `/andrew-kane-gem-writer`.

1. Read **`skills/andrew-kane-gem-writer/SKILL.md`** relative to this plugin root (the directory that contains both `skills/` and `commands/`). Use the Read tool and resolve against the plugin install root if the workspace cwd differs.
2. Execute that skill end-to-end. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `andrew-kane-gem-writer`
