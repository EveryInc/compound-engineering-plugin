<!-- compound-plugin:cursor-command generated -->

# ce-debug

Invokes **`ce-debug`** (compound-engineering). Slash command: `/ce-debug`.

1. Read **`skills/ce-debug/SKILL.md`** relative to this plugin root (the directory that contains both `skills/` and `commands/`). Use the Read tool and resolve against the plugin install root if the workspace cwd differs.
2. Execute that skill end-to-end. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `ce-debug`
