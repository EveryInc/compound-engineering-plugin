<!-- compound-plugin:cursor-command generated -->

# ce-slack-research

Invokes **`ce-slack-research`** (compound-engineering). Slash command: `/ce-slack-research`.

1. Read **`skills/ce-slack-research/SKILL.md`** relative to this plugin root (the directory that contains both `skills/` and `commands/`). Use the Read tool and resolve against the plugin install root if the workspace cwd differs.
2. Execute that skill end-to-end. User input: **$1**. If `$1` is empty, follow the skill's instructions for missing input.

Skill directory: `ce-slack-research`
