Envelope fixtures (`*.json` except `live-tools.json`) copied verbatim from https://github.com/kieranklaassen/riffrec `src/live/fixtures/` at `main` @ `bff7ffe5febf686151f9edd517d2c7ac636d763c` (re-verified byte-identical on 2026-09-16; first copied from `972fcb6f` on 2026-09-15, unchanged since, and untouched by PR #29). The contract reference for these tests is that commit's `docs/live-stream-contract.md` and `src/live/contract.ts` (`CHECKPOINT_TRIGGERS` incl. `mode_change`, `ALWAYS_WAKE_TRIGGERS` = `answer`, `mode_change`, `final`).

`live-tools.json` and `interviewer-instructions.txt` are dumps of riffrec's `LIVE_TOOLS` (`src/live/tools.ts`) and `DEFAULT_INTERVIEWER_INSTRUCTIONS` (`src/live/realtime/persona.ts`) at PR #29 head `55be2841c1c255c8e8d1e0217d051d6b3fc05409` (`cursor/live-interviewer-screen-context-745c`), produced with `JSON.stringify(LIVE_TOOLS, null, 2)` and the raw string. The mint test pins `scripts/live-endpoint.js`'s `INTERVIEWER_TOOLS` and `INTERVIEWER_PERSONA` to them byte for byte, because the page reconciles the minted session after connect and patches only what differs from these. Regenerate both when riffrec changes either file and update the commit here.
