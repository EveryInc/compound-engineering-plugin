# Sort preference requirements

Signed-in members can choose ascending or descending order for their current list. The choice applies immediately, is stored in the existing preference record, and is restored on reload. Invalid saved values fall back to ascending. Tests cover both choices, reload, and invalid saved state.
