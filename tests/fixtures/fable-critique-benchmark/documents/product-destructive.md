# Bulk cleanup requirements

The table toolbar opens with every row selected and the primary action labeled Delete selected. Activating it immediately deletes all selected records. Undo and confirmation are outside scope.
