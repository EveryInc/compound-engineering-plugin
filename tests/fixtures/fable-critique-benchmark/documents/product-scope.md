# Workspace analytics goal

Goal: give every workspace a weekly analytics digest. This change adds a digest job for the primary workspace only; secondary workspaces and workspace selection are outside scope. Success is one digest sent for the primary workspace.
